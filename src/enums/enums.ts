// Events
export enum EventNames {
	CARD_BTN_CLICKED,
}

// Header
export enum AppBarDensity {
	COMPACT = 48,
	COMFORTABLE = 56,
	DEFAULT = 64,
	PROMINENT = 128,
}

// Common
/// News
export enum FilterSortBy {
	ASCENDING,
	DESCENDING,
}
export enum FilterRefineBy {
	TYPE = "type",
	DATE = "date",
}

// Footer
