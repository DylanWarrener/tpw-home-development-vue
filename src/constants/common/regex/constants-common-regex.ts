// Interfaces
//import ICommonRegex from "@interfaces/common/regex/interfaces-common-regex";

// Regex
export const stringPatterns = {
	isGreaterThanThreeCharacters: /^([a-z]{3,}(\s+)?)+$/,
	isGreaterThanThreeWords: /^([a-z]{3,}(\s+)?){4,}$/,
};
export const intPatterns = {};
